apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "triviaWaterHelmChart.fullname" . }}-test-connection"
  labels:
    {{- include "triviaWaterHelmChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "triviaWaterHelmChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
