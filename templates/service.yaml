apiVersion: v1
kind: Service
metadata:
  name: {{ include "triviaWaterHelmChart.fullname" . }}
  labels:
    {{- include "triviaWaterHelmChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "triviaWaterHelmChart.selectorLabels" . | nindent 4 }}
