default:
  resources:
    requests:
      memory: "50mi"
      cpu: "256m"
    limits:
      memory: "100mi"
      cpu: "512m"

client:
  service: 
    type: ClusterIP
    port: 80
  port: 5176
  image:
    repository: rivkayo/triviawater
    tag: 1.6
    pullPolicy: IfNotPresent #if img not in pod
  replicaCount: 3

server:
  service: 
    type: ClusterIP
    port: 8080
  port: 2024
  image:
    repository: rivkayo/triviawaterserver
    tag: 1.1
    pullPolicy: IfNotPresent
  replicaCount: 3

database:
  service: 
    type: ClusterIP
    port: 3306
  mysql:
    DB_HOST: localhost
    DB_PORT: 3306
    DB_NAME: trivia_water
    image:
        repository: rivkayo/triviawaterdatabase
        tag: 1.0
        pullPolicy: IfNotPresent
      replicaCount: 1

service:
  type: ClusterIP
  clientPort: 8080
  serverPort: 8081

ingress:
  enabled: true
  host: myapp.example.com
  paths:
    - /client
    - /server
